[project]
name = "route-optimizer-backend"
version = "0.1.0"
description = "Backend para Optimizador de Rutas con Algoritmo Genético"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "functions-framework>=3.4.0",
    "requests>=2.31.0",
    "firebase-admin>=6.5.0",
    "python-dotenv>=1.0.0",
]
